# TF Pre-Deploy Firewall — default configuration.
# Override path with --config or the SCANNER_CONFIG env var.

# Minimum severity that blocks the PR (exit code 1).
# One of: low, medium, high, critical
block_threshold: high
