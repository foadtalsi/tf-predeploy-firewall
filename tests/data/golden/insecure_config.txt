insecure_config.tf:10 | missing_lifecycle | medium | aws_db_instance.analytics | aws_db_instance is a stateful/critical resource with no lifecycle { prevent_destroy = true } guard | suggestion=lifecycle {\n  prevent_destroy = true\n} | fix=10-10:resource "aws_db_instance" "analytics" {\n  lifecycle {\n    prevent_destroy = true\n  }
insecure_config.tf:109 | permissive_iam | high | aws_ecr_repository_policy.app | the policy on aws_ecr_repository_policy.app names Principal "*" with no Condition — this grants the listed actions to every AWS account on earth, not to every principal in yours | suggestion=# Name the accounts or roles, or keep "*" and narrow it:\nCondition = {\n  StringEquals = { "aws:PrincipalOrgID" = "o-example" }\n}
insecure_config.tf:120 | permissive_iam | high | data.aws_iam_policy_document.admin | statement in data.aws_iam_policy_document.admin sets actions = ["*"] — every action in every AWS service, including the IAM calls that would let a holder grant itself anything else | suggestion=actions = ["s3:GetObject", "s3:PutObject"]
insecure_config.tf:13 | public_exposure | high | aws_db_instance.analytics | aws_db_instance.analytics sets publicly_accessible = true — this attaches a public IP and puts the data store on the internet, where only its security group stands between it and the world | suggestion=publicly_accessible = false\n\n# and reach it from inside the VPC instead — a bastion, VPN, or\n# SSM Session Manager port-forward. | fix=13-13:  publicly_accessible = false
insecure_config.tf:14 | encryption_disabled | high | aws_db_instance.analytics | storage_encrypted = false disables encryption at rest on aws_db_instance.analytics — on most types this cannot be turned on later without replacing the resource, so it is decided here or not at all
insecure_config.tf:15 | missing_lifecycle | medium | aws_db_instance.analytics | skip_final_snapshot = true on aws_db_instance.analytics — a destroy of this database keeps nothing, so recovery after a mistaken apply is impossible rather than slow | suggestion=skip_final_snapshot       = false\nfinal_snapshot_identifier = "analytics-final"
insecure_config.tf:20 | public_exposure | high | aws_s3_bucket_public_access_block.reports | block_public_acls = false on aws_s3_bucket_public_access_block.reports — this resource exists to stop the bucket being made public by any future ACL or policy, and this switch removes that guarantee | fix=20-20:  block_public_acls = true
insecure_config.tf:21 | public_exposure | high | aws_s3_bucket_public_access_block.reports | block_public_policy = false on aws_s3_bucket_public_access_block.reports — this resource exists to stop the bucket being made public by any future ACL or policy, and this switch removes that guarantee | fix=21-21:  block_public_policy = true
insecure_config.tf:22 | public_exposure | high | aws_s3_bucket_public_access_block.reports | ignore_public_acls = false on aws_s3_bucket_public_access_block.reports — this resource exists to stop the bucket being made public by any future ACL or policy, and this switch removes that guarantee | fix=22-22:  ignore_public_acls = true
insecure_config.tf:23 | public_exposure | high | aws_s3_bucket_public_access_block.reports | restrict_public_buckets = false on aws_s3_bucket_public_access_block.reports — this resource exists to stop the bucket being made public by any future ACL or policy, and this switch removes that guarantee | fix=23-23:  restrict_public_buckets = true
insecure_config.tf:28 | public_exposure | high | aws_s3_bucket_acl.reports | acl = "public-read" grants access outside this account — "public-read" and "public-read-write" are open to anyone on the internet, and "authenticated-read" is open to every AWS user everywhere, not just yours
insecure_config.tf:35 | public_exposure | high | aws_s3_bucket_acl.internal | acl = "authenticated-read" grants access outside this account — "public-read" and "public-read-write" are open to anyone on the internet, and "authenticated-read" is open to every AWS user everywhere, not just yours
insecure_config.tf:43 | public_exposure | medium | aws_instance.bastion | metadata_options.http_tokens = "optional" on aws_instance.bastion allows IMDSv1 — any SSRF in software running on this instance can then read its IAM credentials from 169.254.169.254 | suggestion=metadata_options {\n  http_tokens = "required"\n} | fix=43-43:    http_tokens = "required"
insecure_config.tf:47 | missing_lifecycle | medium | aws_ebs_volume.data | aws_ebs_volume is a stateful/critical resource with no lifecycle { prevent_destroy = true } guard | suggestion=lifecycle {\n  prevent_destroy = true\n} | fix=47-47:resource "aws_ebs_volume" "data" {\n  lifecycle {\n    prevent_destroy = true\n  }
insecure_config.tf:50 | encryption_disabled | high | aws_ebs_volume.data | encrypted = false disables encryption at rest on aws_ebs_volume.data — on most types this cannot be turned on later without replacing the resource, so it is decided here or not at all
insecure_config.tf:53 | missing_lifecycle | medium | aws_elasticache_replication_group.cache | aws_elasticache_replication_group is a stateful/critical resource with no lifecycle { prevent_destroy = true } guard | suggestion=lifecycle {\n  prevent_destroy = true\n} | fix=53-53:resource "aws_elasticache_replication_group" "cache" {\n  lifecycle {\n    prevent_destroy = true\n  }
insecure_config.tf:55 | encryption_disabled | high | aws_elasticache_replication_group.cache | transit_encryption_enabled = false disables encryption in transit on aws_elasticache_replication_group.cache — traffic to it is readable by anything on the network path between client and service | fix=55-55:  transit_encryption_enabled = true
insecure_config.tf:56 | encryption_disabled | high | aws_elasticache_replication_group.cache | at_rest_encryption_enabled = false disables encryption at rest on aws_elasticache_replication_group.cache — on most types this cannot be turned on later without replacing the resource, so it is decided here or not at all
insecure_config.tf:63 | encryption_disabled | medium | aws_lb_listener.legacy | ssl_policy = "ELBSecurityPolicy-TLS-1-0-2015-04" permits TLS 1.0/1.1 on aws_lb_listener.legacy — both are deprecated by RFC 8996, outside the PCI DSS accepted set, and refused by current browsers
insecure_config.tf:69 | audit_disabled | high | aws_cloudtrail.main | enable_logging = false on aws_cloudtrail.main disables the audit trail itself — this is the record used to answer what happened, and it is being switched off in configuration rather than by an attacker | fix=69-69:  enable_logging = true
insecure_config.tf:81 | permissive_iam | high | aws_iam_role_policy.admin | the IAM policy on aws_iam_role_policy.admin grants Action "*" on Resource "*" — this is unrestricted administrator access to the account, which is almost never what a service needs | suggestion=# Name the actions this actually needs, and the resources it needs them on:\nAction   = ["s3:GetObject", "s3:PutObject"]\nResource = ["${aws_s3_bucket.data.arn}/*"]
insecure_config.tf:95 | permissive_iam | high | aws_iam_role.anyone | the policy on aws_iam_role.anyone names Principal "*" with no Condition — this grants the listed actions to every AWS account on earth, not to every principal in yours | suggestion=# Name the accounts or roles, or keep "*" and narrow it:\nCondition = {\n  StringEquals = { "aws:PrincipalOrgID" = "o-example" }\n}
insecure_config_clean.tf:13 | missing_lifecycle | medium | aws_db_instance.prod | aws_db_instance is a stateful/critical resource with no lifecycle { prevent_destroy = true } guard | suggestion=lifecycle {\n  prevent_destroy = true\n} | fix=13-13:resource "aws_db_instance" "prod" {\n  lifecycle {\n    prevent_destroy = true\n  }
