credential_values.tf:14 | tutorial_pattern | critical | aws_lambda_function.with_jwt | "description" value matches pattern: JWT token — remove from source and use a secret reference
credential_values.tf:5 | tutorial_pattern | critical | aws_instance.leaky | "user_data" value matches pattern: AWS access key ID (AKIA…) — remove from source and use a secret reference
nested_block_cidr.tf:9 | tutorial_pattern | high | aws_security_group.wide_open | "cidr_blocks (inside ingress block)" includes 0.0.0.0/0 (open to the entire internet) — common tutorial copy-paste, narrow this range
tutorial_golden.tf:142 | tutorial_pattern | high | aws_ssm_parameter.unknown_vendor_token | "value" value is a high-entropy string (4.8 bits/char over 27 chars) — this is the statistical signature of a machine-generated secret; if it is one, move it out of source and rotate it
tutorial_golden.tf:15 | tutorial_pattern | critical | aws_db_instance.primary | "password" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "primary_password" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_db_instance.primary:\npassword = var.primary_password | fix=15-15:  password = var.primary_password | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_primary_password, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "primary_password" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:16 | tutorial_pattern | critical | aws_db_instance.primary | "administrator_login_password" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "primary_administrator_login_password" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_db_instance.primary:\nadministrator_login_password = var.primary_administrator_login_password | fix=16-16:  administrator_login_password = var.primary_administrator_login_password | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_primary_administrator_login_password, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "primary_administrator_login_password" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:167 | tutorial_pattern | high | aws_security_group_rule.ssh_from_anywhere | "cidr_blocks" includes 0.0.0.0/0 (open to the entire internet) — common tutorial copy-paste, narrow this range
tutorial_golden.tf:178 | tutorial_pattern | high | aws_security_group.mixed | "cidr_blocks (inside ingress block)" includes 0.0.0.0/0 (open to the entire internet) — common tutorial copy-paste, narrow this range
tutorial_golden.tf:193 | tutorial_pattern | low | aws_s3_bucket.example | resource local name "example" looks like a tutorial placeholder, not a deliberate identifier
tutorial_golden.tf:194 | tutorial_pattern | low | aws_s3_bucket.example | bucket = "my-bucket" looks like a tutorial placeholder value
tutorial_golden.tf:197 | tutorial_pattern | low | aws_s3_bucket.test_data | resource local name "test_data" looks like a tutorial placeholder, not a deliberate identifier
tutorial_golden.tf:202 | tutorial_pattern | low | aws_db_instance.deliberate | identifier = "demo-cluster" looks like a tutorial placeholder value
tutorial_golden.tf:21 | tutorial_pattern | critical | azurerm_key_vault_secret.app | "client_secret" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "app_client_secret" {\n  type      = string\n  sensitive = true\n}\n\n# in azurerm_key_vault_secret.app:\nclient_secret = var.app_client_secret | fix=21-21:  client_secret = var.app_client_secret | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_app_client_secret, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "app_client_secret" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:22 | tutorial_pattern | critical | azurerm_key_vault_secret.app | "api_key" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "app_api_key" {\n  type      = string\n  sensitive = true\n}\n\n# in azurerm_key_vault_secret.app:\napi_key = var.app_api_key | fix=22-22:  api_key = var.app_api_key | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_app_api_key, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "app_api_key" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:23 | tutorial_pattern | critical | azurerm_key_vault_secret.app | "auth_token" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "app_auth_token" {\n  type      = string\n  sensitive = true\n}\n\n# in azurerm_key_vault_secret.app:\nauth_token = var.app_auth_token | fix=23-23:  auth_token = var.app_auth_token | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_app_auth_token, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "app_auth_token" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:28 | tutorial_pattern | critical | aws_dms_endpoint.warehouse | "connection_string" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "warehouse_connection_string" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_dms_endpoint.warehouse:\nconnection_string = var.warehouse_connection_string | fix=28-28:  connection_string = var.warehouse_connection_string | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_warehouse_connection_string, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "warehouse_connection_string" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:58 | tutorial_pattern | critical | aws_db_instance.via_variable | "password" resolves to a hardcoded string literal (via var.db_password), not a secret reference — credentials must not be committed in plain text | suggestion=variable "via_variable_password" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_db_instance.via_variable:\npassword = var.via_variable_password
tutorial_golden.tf:66 | tutorial_pattern | critical | aws_instance.with_access_key | "user_data" value matches pattern: AWS access key ID (AKIA…) — remove from source and use a secret reference
tutorial_golden.tf:72 | tutorial_pattern | critical | aws_instance.with_secret_key | "user_data" value matches pattern: possible AWS secret key (40-char base64) — remove from source and use a secret reference
tutorial_golden.tf:80 | tutorial_pattern | critical | aws_lambda_function.with_jwt_value | "description" value matches pattern: JWT token — remove from source and use a secret reference
tutorial_golden.tf:86 | tutorial_pattern | critical | aws_ssm_parameter.with_pem | "value" value matches pattern: PEM private key — remove from source and use a secret reference
tutorial_golden.tf:92 | tutorial_pattern | critical | aws_ssm_parameter.with_forge_token | "value" value matches pattern: GitHub personal access token — remove from source and use a secret reference
tutorial_golden.tf:98 | tutorial_pattern | critical | aws_ssm_parameter.with_hex_digest | "value" value matches pattern: high-entropy hex string (possible secret) — remove from source and use a secret reference
tutorial_pattern.tf:1 | tutorial_pattern | low | aws_db_instance.example | resource local name "example" looks like a tutorial placeholder, not a deliberate identifier
tutorial_pattern.tf:12 | tutorial_pattern | high | aws_security_group_rule.open | "cidr_blocks" includes 0.0.0.0/0 (open to the entire internet) — common tutorial copy-paste, narrow this range
tutorial_pattern.tf:2 | tutorial_pattern | low | aws_db_instance.example | identifier = "example" looks like a tutorial placeholder value
tutorial_pattern.tf:4 | tutorial_pattern | critical | aws_db_instance.example | "password" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "example_password" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_db_instance.example:\npassword = var.example_password | fix=4-4:  password = var.example_password | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_example_password, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "example_password" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
