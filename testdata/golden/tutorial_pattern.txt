credential_values.tf:15 | tutorial_pattern | critical | aws_lambda_function.with_jwt | "description" value matches pattern: JWT token — remove from source and use a secret reference
credential_values.tf:5 | tutorial_pattern | critical | aws_instance.leaky | "user_data" value matches pattern: AWS access key ID (AKIA…) — remove from source and use a secret reference
nested_block_cidr.tf:9 | tutorial_pattern | high | aws_security_group.wide_open | "cidr_blocks (inside ingress block)" includes 0.0.0.0/0 (open to the entire internet) — common tutorial copy-paste, narrow this range
tutorial_golden.tf:136 | tutorial_pattern | critical | aws_db_instance.via_variable | "password" resolves to a hardcoded string literal (via var.db_password), not a secret reference — credentials must not be committed in plain text | suggestion=variable "via_variable_password" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_db_instance.via_variable:\npassword = var.via_variable_password
tutorial_golden.tf:144 | tutorial_pattern | critical | aws_instance.with_access_key | "user_data" value matches pattern: AWS access key ID (AKIA…) — remove from source and use a secret reference
tutorial_golden.tf:150 | tutorial_pattern | critical | aws_instance.with_secret_key | "user_data" value matches pattern: possible AWS secret key (40-char base64) — remove from source and use a secret reference
tutorial_golden.tf:157 | tutorial_pattern | critical | aws_lambda_function.with_jwt_value | "description" value matches pattern: JWT token — remove from source and use a secret reference
tutorial_golden.tf:163 | tutorial_pattern | critical | aws_ssm_parameter.with_pem | "value" value matches pattern: PEM private key — remove from source and use a secret reference
tutorial_golden.tf:169 | tutorial_pattern | critical | aws_ssm_parameter.with_forge_token | "value" value matches pattern: GitHub personal access token — remove from source and use a secret reference
tutorial_golden.tf:175 | tutorial_pattern | critical | aws_ssm_parameter.with_hex_digest | "value" value matches pattern: high-entropy hex string (possible secret) — remove from source and use a secret reference
tutorial_golden.tf:184 | tutorial_pattern | high | aws_ssm_parameter.unknown_vendor_token | "value" value is a high-entropy string (4.8 bits/char over 27 chars) — this is the statistical signature of a machine-generated secret; if it is one, move it out of source and rotate it
tutorial_golden.tf:23 | tutorial_pattern | critical | aws_db_instance.primary | "password" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "primary_password" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_db_instance.primary:\npassword = var.primary_password | fix=23-23:  password = var.primary_password | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_primary_password, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "primary_password" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:250 | tutorial_pattern | high | aws_security_group_rule.ssh_from_anywhere | "cidr_blocks" includes 0.0.0.0/0 (open to the entire internet) — common tutorial copy-paste, narrow this range
tutorial_golden.tf:261 | tutorial_pattern | high | aws_security_group.mixed | "cidr_blocks (inside ingress block)" includes 0.0.0.0/0 (open to the entire internet) — common tutorial copy-paste, narrow this range
tutorial_golden.tf:276 | tutorial_pattern | low | aws_s3_bucket.example | resource local name "example" looks like a tutorial placeholder, not a deliberate identifier
tutorial_golden.tf:277 | tutorial_pattern | low | aws_s3_bucket.example | bucket = "my-bucket" looks like a tutorial placeholder value
tutorial_golden.tf:280 | tutorial_pattern | low | aws_s3_bucket.test_data | resource local name "test_data" looks like a tutorial placeholder, not a deliberate identifier
tutorial_golden.tf:285 | tutorial_pattern | low | aws_db_instance.deliberate | identifier = "demo-cluster" looks like a tutorial placeholder value
tutorial_golden.tf:35 | tutorial_pattern | critical | azurerm_mssql_server.reporting | "administrator_login_password" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "reporting_administrator_login_password" {\n  type      = string\n  sensitive = true\n}\n\n# in azurerm_mssql_server.reporting:\nadministrator_login_password = var.reporting_administrator_login_password | fix=35-35:  administrator_login_password = var.reporting_administrator_login_password | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_reporting_administrator_login_password, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "reporting_administrator_login_password" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:41 | tutorial_pattern | critical | aws_pinpoint_baidu_channel.notifications | "api_key" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "notifications_api_key" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_pinpoint_baidu_channel.notifications:\napi_key = var.notifications_api_key | fix=41-41:  api_key = var.notifications_api_key | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_notifications_api_key, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "notifications_api_key" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:42 | tutorial_pattern | critical | aws_pinpoint_baidu_channel.notifications | "secret_key" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "notifications_secret_key" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_pinpoint_baidu_channel.notifications:\nsecret_key = var.notifications_secret_key | fix=42-42:  secret_key = var.notifications_secret_key | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_notifications_secret_key, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "notifications_secret_key" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:48 | tutorial_pattern | critical | aws_pinpoint_adm_channel.amazon_devices | "client_secret" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "amazon_devices_client_secret" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_pinpoint_adm_channel.amazon_devices:\nclient_secret = var.amazon_devices_client_secret | fix=48-48:  client_secret = var.amazon_devices_client_secret | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_amazon_devices_client_secret, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "amazon_devices_client_secret" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:54 | tutorial_pattern | critical | aws_elasticache_replication_group.sessions | "auth_token" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "sessions_auth_token" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_elasticache_replication_group.sessions:\nauth_token = var.sessions_auth_token | fix=54-54:  auth_token = var.sessions_auth_token | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_sessions_auth_token, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "sessions_auth_token" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:58 | tutorial_pattern | critical | aws_workspaces_connection_alias.desks | "connection_string" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "desks_connection_string" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_workspaces_connection_alias.desks:\nconnection_string = var.desks_connection_string | fix=58-58:  connection_string = var.desks_connection_string | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_desks_connection_string, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "desks_connection_string" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:62 | tutorial_pattern | critical | aws_acm_certificate.internal | "private_key" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "internal_private_key" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_acm_certificate.internal:\nprivate_key = var.internal_private_key | fix=62-62:  private_key = var.internal_private_key | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_internal_private_key, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "internal_private_key" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_golden.tf:85 | tutorial_pattern | critical | azurerm_kubernetes_cluster.prod | (inside service_principal block) "client_secret" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "prod_service_principal_client_secret" {\n  type      = string\n  sensitive = true\n}\n\n# in azurerm_kubernetes_cluster.prod:\nclient_secret = var.prod_service_principal_client_secret | fix=85-85:    client_secret = var.prod_service_principal_client_secret | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_prod_service_principal_client_secret, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "prod_service_principal_client_secret" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
tutorial_pattern.tf:1 | tutorial_pattern | low | aws_db_instance.example | resource local name "example" looks like a tutorial placeholder, not a deliberate identifier
tutorial_pattern.tf:14 | tutorial_pattern | high | aws_security_group_rule.open | "cidr_blocks" includes 0.0.0.0/0 (open to the entire internet) — common tutorial copy-paste, narrow this range
tutorial_pattern.tf:2 | tutorial_pattern | low | aws_db_instance.example | identifier = "example" looks like a tutorial placeholder value
tutorial_pattern.tf:4 | tutorial_pattern | critical | aws_db_instance.example | "password" resolves to a hardcoded string literal, not a secret reference — credentials must not be committed in plain text | suggestion=variable "example_password" {\n  type      = string\n  sensitive = true\n}\n\n# in aws_db_instance.example:\npassword = var.example_password | fix=4-4:  password = var.example_password | note=This also needs the variable declared, and its value supplied outside the repository (TF_VAR_example_password, a tfvars file that isn't committed, or your secret manager):\n\n```hcl\nvariable "example_password" {\n  type      = string\n  sensitive = true\n}\n```\n\n**The old value is still in this branch's git history — rotate it.**
